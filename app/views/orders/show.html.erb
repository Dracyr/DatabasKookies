<h1>Order</h1>

<p>
  <strong>Customer:</strong>
  <%= @order.customer %>
</p>

<p>
  <strong>Delivered at:</strong>
  <%= @order.delivered_at %>
</p>

<p>
  <strong>Products:</strong>
  <ul>
    <% @order.order_products.each do |op| %>
      <li>
        <b><%= op.name %></b>: <%= op.quantity %>
      </li>
    <% end %>
  </ul>
</p>


<%= link_to 'Edit', edit_order_path(@order) %> |
<%= link_to 'Back', orders_path %>
