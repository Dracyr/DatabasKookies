<%= bootstrap_form_for(@pallet) do |f| %>
  <%= f.collection_select :product_id, Product.all, :id, :name, {}, {class: 'select2'} %>

  <%= f.form_group do %>
    <%= f.submit %>
  <% end %>
<% end %>
